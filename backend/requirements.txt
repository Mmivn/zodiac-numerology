# Multi-provider AI gateway (Gemini -> Groq -> Mistral -> Cloudflare Workers AI
# -> OpenAI last-resort), installed from its own GitHub repo instead of
# duplicating provider code here. See ../ai_service.py (imported unmodified
# by main.py) and https://github.com/Mmivn/ALL_API for the source.
#
# Pinned to a specific commit for reproducible builds. To pick up a newer
# ALL_API version, bump this SHA to the desired commit on that repo's main
# branch — no other change needed here. Kept identical to the repo-root
# requirements.txt (used by streamlit_app.py) on purpose — same gateway,
# same pin, for both front ends.
git+https://github.com/Mmivn/ALL_API.git@6dbf58033763acbf04869b72023102a71e69f87a#egg=all-api

python-dotenv>=1.0

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# Dev/test only.
pytest>=8.0
httpx>=0.27
